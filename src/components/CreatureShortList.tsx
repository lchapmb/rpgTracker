// import context
import { useCreaturesContext } from "../views/App";

const { creaturesArr, setCreaturesArr } = useCreaturesContext();

export default function CreatureShortList() {
  return <div>CreatureShortList</div>;
}
